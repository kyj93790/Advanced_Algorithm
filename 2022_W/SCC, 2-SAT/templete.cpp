#include <iostream>
#include <vector>
#include <stack>
#include <algorithm>
using namespace std;
#define p(a, b) make_pair(a, b)

typedef long long ll;
void swap(ll *a, ll *b) {
    ll temp;
    temp = *a;
    *a = *b;
    *b = temp;
}

int main(void) {
    cin.tie(0); cout.tie(0); ios::sync_with_stdio(0);
    
    return 0;
}